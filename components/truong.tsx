import React from "react";
import Box from "@mui/system/Box";
type Props = {};

const Truong = () => {
  <Box component="section" sx={{ p: 2, border: "1px dashed grey" }}>
    This is a section container
  </Box>;
};

export const truong = Truong;
